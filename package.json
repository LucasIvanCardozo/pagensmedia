{
  "name": "pagensmedia",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && echo 'pagensmedia.com.ar' > dist/CNAME && gh-pages -d dist"
  },
  "dependencies": {
    "astro": "^6.1.5"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
